#include "main.h"
/**
 * add - a function that adds two integers and prints the result
 *
 * @n: an integer to be added to m.
 * @m: an integer to be added to n.
 * Return: the result.
 */
int add(int n, int m)
{
	int result;

	result = n + m;

	return (result);
}
